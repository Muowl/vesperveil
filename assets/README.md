# Vesperveil icon

Approved variant: **Espectral**.

- Displacement: 1.4 px at a 128 px reference size (5.6 SVG units).
- Chromatic opacity: 60%.
- Stroke width: 8 SVG units.
- Chromatic colors: rose `#DB91A3` and lavender `#B5A5CF`.
- Effect enabled on the eye and iris.
- Background: `#171316`; eye: `#C65A70`; iris: `#D7B77D`.

`icon.svg` is the editable source. `icon.png` is a 512 × 512 raster export
referenced by the extension manifest. The settings are preserved in
`icon-settings.json`. Geometry matches the approved interactive study;
no extra corner ornaments are included.
